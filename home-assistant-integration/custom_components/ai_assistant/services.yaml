ask_question:
  name: Ask Question
  description: Ask a question to the AI Assistant
  fields:
    question:
      name: Question
      description: The question to ask
      required: true
      selector:
        text:

reset:
  name: Reset
  description: Reset the AI Assistant conversation count

